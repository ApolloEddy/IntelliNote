name: intellinote
version: 0.1.0
publish_to: none

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  file_picker: ^8.0.0
  http: ^1.6.0
  crypto: ^3.0.7

flutter:
  uses-material-design: true
